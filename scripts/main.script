local window=require("scripts.window")
local monarch = require "monarch.monarch"

function init(self)
	window.center()
	window.resize()
	
	
	msg.post("@render:", "use_fixed_fit_projection", { near = -1, far = 1 })
	msg.post("@render:/", "clear_color", { color = vmath.vector4(0.1, 0.12, 0.21,1.0) })
	monarch.show(hash("game"))
end

